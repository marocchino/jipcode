{
  "name": "jipcode",
  "version": "2.0.37",
  "description": "jipcode porting from ruby",
  "main": "dist/index.js",
  "author": "marocchino",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/jest": "^28.1.7",
    "csv2json": "^2.0.2",
    "esbuild": "^0.15.4",
    "jest": "^28.1.3",
    "prettier": "^2.7.1",
    "ts-jest": "^28.0.8",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "test": "jest",
    "build": "esbuild index.ts --bundle --minify --format=esm --sourcemap --outfile=dist/index.js"
  }
}
